{
  "name": "aws-lambda-ts-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node_modules/typescript/bin/tsc",
    "test": "jest",
    "deploy": "npm run build && sam build && sam deploy --stack-name your-function-name --s3-bucket your-bucket-name --capabilities CAPABILITY_IAM"
  },
  "files": [
    "/handlers"
  ],
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "@types/aws-lambda": "^8.10.72",
    "@types/jest": "^27.0.3",
    "@types/node": "^14.17.32",
    "aws-sdk": "^2.1031.0",
    "jest": "^27.3.1",
    "jest-cli": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.5.2"
  },
  "license": "ISC"
}
